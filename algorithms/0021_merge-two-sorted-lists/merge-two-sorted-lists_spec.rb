# frozen_string_literal: true

require '../0020_valid-parentheses/valid-parentheses'

RSpec.describe do

  context '' do
    # it 't1' do
    #   expect(is_valid("(]")).to eq false
    # end
    # it 't2' do
    #   expect(is_valid("()")).to eq true
    # end
    # it 't3' do
    #   expect(remove_nth_from_end([1, 2], 1)).to eq [1]
    # end
  end
end
